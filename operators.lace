struct Vector {
	x: float
	y: float
	z: float
	def self + vec: Vector* -> Vector {
		let result: Vector
		result.x = self.x + vec.x
		result.y = self.y + vec.y
		result.z = self.z + vec.z
		return result
	}
	def self - vec: Vector* -> Vector {
		let result: Vector
		result.x = self.x - vec.x
		result.y = self.y - vec.y
		result.z = self.z - vec.z
		return result
	}
}
fun main (argc : int, argv : string*) -> int {
	let one : Vector
	let two : Vector
	let three = one + two
	return 0
}
